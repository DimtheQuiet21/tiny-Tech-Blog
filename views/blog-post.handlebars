<br>
<header class = "d-flex flex-column justify-content-center">
    <div class = "d-flex flex-row justify-content-between">
        <h2 id = "blogpost_title">{{blog_post.title}}</h2>
        {{#if owner}}
        <div class = "d-flex flex-row">
            <button class = "btn btn-secondary" onclick = "update_post(event)">Update</button>
            <button class = "btn btn-danger" onclick = "delete_post(event)">Delete</button>
        </div>
        {{/if}}
    </div>
    <h4>Created by: {{blog_post.user.username}}</h4>
</header>
<div>
    <h4>Created On: {{blog_post.createdAt}}</h4>
    <h6>Last Updated: {{blog_post.updatedAt}}</h6>
</div>
<br>

<div>
    <p id = "blogpost_description">{{blog_post.description}}</p>
</div>

<div id = "comment_section">
{{#if logged_in}}
    <button class = "btn btn-primary" id = "comment_btn" onclick = "create_comment()">Leave a Comment</button>
    <br></br>
{{else}}
    <button class = "btn btn-primary" id = "sign_in_btn" onclick="window.location.href='/login'">Sign in to Leave a Comment</button>
    <br></br>
{{/if}}



{{#each blog_post.Comments as |comment|}}
{{> comment-detail}}
<br>
{{/each}}

</div>


